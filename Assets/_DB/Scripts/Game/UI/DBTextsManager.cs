using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DB_Core;

namespace DB_Game
{
    public class DBTextsManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}